numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
streamlit>=1.30
pytest>=7.4

# Версия закреплена точно. В ветке 1.x pgmpy переименовал часть API
# (BayesianNetwork -> DiscreteBayesianNetwork, MaximumLikelihoodEstimator ->
# DiscreteMLE, StructureScore -> structure_score), поэтому диапазон вида
# `pgmpy>=0.1.24` означал бы, что сборка ломается в произвольный момент.
#
# Ядро проекта (model, decision, sensitivity, structure_learning, calibration,
# reference_inference) от pgmpy не зависит вовсе и работает без него -- это
# отдельно проверяется джобой `core` в CI. pgmpy нужен только для
# src/network.py и src/learning.py, где служит второй, независимой
# реализацией той же сети.
pgmpy==1.1.2

# опционально: если не установлен, src/baselines.py откатывается на
# sklearn.HistGradientBoostingClassifier без ошибок
xgboost>=2.0
